import Foundation

// Minimal skeleton stub: the real dictation agent orchestrator ships in a later PR.
FileHandle.standardError.write(Data("NanoDictateAgent: not implemented in minimal skeleton\n".utf8))
exit(0)