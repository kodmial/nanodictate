// swift-tools-version:5.7
// NanoDictate — minimal main-branch skeleton (pre-PR). Real code arrives via PR.
import PackageDescription

let package = Package(
    name: "NanoDictate",
    platforms: [.macOS(.v12)],
    products: [],
    targets: []
)